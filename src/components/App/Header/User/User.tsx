import React, {useState, useEffect} from 'react';
import './User.scss';

const User = () => {
    const [showMenu,setShowMenu] = useState(false);
    const handleClick = () => {

    }
    return(
        <div>
            <h2></h2>
        </div>
    )
}

export default User